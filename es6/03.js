//let 只在区块内生效
for (let index = 0; index < array.length; index++) {
  const element = array[index];
}

//定义区块
{}

//箭头函数
v => v + 1;
v => ({ k: v });
(x, y) => x + y;
z => {};

//参数赋值
function f(x, y = 7) {}

//传入剩余参数
function f(x, y, ...a) {}







